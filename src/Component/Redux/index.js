import { login,logOut,dataAvailable,changeFirstName,changeLastName } from "./userSlice";
import store from "./store"

export
{
    login,
    logOut,
    dataAvailable,
    changeFirstName,changeLastName,
    store
}